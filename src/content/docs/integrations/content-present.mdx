---
title: '内容展示'
description: '关于内容页面渲染'
order: 5
---

import { MdxRepl } from 'astro-pure/user'

## Shiki 代码

你可以在配置文件 `astro.config.ts` 的 `defineConfig.markdown.shikiConfig` 中更改转换器。

### 基本用法

对于 `.astro` 文件：

```astro
---
import { Code } from 'astro:components'
---

<Code lang='shell' code={`git log --oneline`} />
```

对于 `.mdx` 和 `.md` 文件：

````markdown
```shell
git log --oneline
```
````

### 添加标题

<MdxRepl width='80%'>
```shell title="start.sh"
#!/bin/bash

bun check &
bun dev
```

<Fragment slot='desc'>
````markdown
```shell title="start.sh"
#!/bin/bash

bun check &
bun dev
```
````
</Fragment>
</MdxRepl>

### 显示差异

支持添加和删除行。

<MdxRepl width='80%'>
```shell title="deploy.sh"
#!/bin/bash

bun check
bun lint
bun format # [!code ++]
# Vercel 或其他 CI/CD 不需要
bun run build # [!code --]
git push
```

<Fragment slot='desc'>
````markdown
```shell title="deploy.sh"
#!/bin/bash

bun check
bun lint
bun format # [\!code ++]
# Vercel 或其他 CI/CD 不需要
bun run build # [\!code --]
git push
```
````
</Fragment>
</MdxRepl>

### 显示高亮

<MdxRepl width='80%'>
```html
<!doctype html><html><head><meta name="color-scheme" content="dark"></head><body style='overflow:hidden'>
<!-- 高亮单行 -->
<p style='text-align:center'>君指先跃动の光は、私の一生不変の信仰に、唯私の超电磁炮永生き！</p> <!-- [!code highlight] -->

<!-- 你也可以用单个注释高亮多行： -->
<canvas id="can" width="400" height="400" style="background:Black;display:block;margin:20px auto;"></canvas>
<script> // [!code highlight:5]
var sn=[42,41], dz=43, fx=1, n, score=-1;
// 更多代码...
</script>
</body></html>
```

<Fragment slot='desc'>
````markdown
```html
<!-- 高亮单行 -->
<p>内容</p> <!-- [\!code highlight] -->

<!-- 高亮多行 -->
<script> // [\!code highlight:5]
// 代码...
</script>
```
````
</Fragment>
</MdxRepl>

## 内容过期警告

如果文章足够旧，添加警告消息。

```astro title="src/layouts/BlogPost.astro"
---
// ...
---
  <Hero {data} {remarkPluginFrontmatter} slot='header'>
    <Fragment slot='description'>
      {!isDraft && enableComment && <PageInfo comment class='mt-1' />}
      {
        Math.floor((Date.now() - new Date(articleDate).getTime()) / 86400000) > 365 && (
          <p class='mt-1 text-sm italic text-primary' style='--primary:41 90% 50%'>
            本文最后更新于一年前。部分信息可能已过时。
          </p>
        )
      }
    </Fragment>
  </Hero>
```

渲染效果：

{
  <p class='mt-1 text-sm italic text-primary' style='--primary:41 90% 50%'>
    本文最后更新于一年前。部分信息可能已过时。
  </p>
}

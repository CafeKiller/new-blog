---
title: '更新主题'
description: '将 astro theme pure 更新到最新版本'
order: 1
---

import { Aside } from 'astro-pure/user'

<Aside type='caution'>
  本主题更新频繁，更新主题可能会有些复杂。请仔细阅读以下破坏性更改。
</Aside>

## 破坏性更改

### v4.1.2

- 将 `src/pages/terms/list.astro` 移动到 `src/pages/terms/index.astro`。

### v4.1.1

- 将 shiki 自定义转换器移动到 `src/plugins/shiki-custom-transformers.ts`。
- 将 shiki 官方转换器移动到 `src/plugins/shiki-offical/transformers.ts`。
- 改进配置文件，新增排序和注释。这将影响 `src/site.config.ts` 和 `astro.config.ts`（[09dc44d](https://github.com/cworld1/astro-theme-pure/commit/09dc44d87987f5f777b21ea642ef8cc9db11e592)）。
- 添加默认折叠代码功能（[7281077](https://github.com/cworld1/astro-theme-pure/commit/7281077b31b7f1bb61bf6eb9a865aae8835cf0e3)，[22b8847](https://github.com/cworld1/astro-theme-pure/commit/22b884789724c1127c7b5a5d1639fd5b0e6a8201)）
- 重构调色板和边框圆角样式。如果你使用 `--primary-foreground` 这样的颜色，请更新为 `--card`。

### v4.1.0

- 将自定义 css 文件 `global.css` 移动到 `src/assets/styles/global.css`。
- 将字体加载器更改为 [Astro 内置字体加载器](https://docs.astro.build/en/reference/experimental-flags/fonts/)。

### v4.0.9

- 支持缓存友链头像（Arthals）

### v4.0.8

无破坏性更改。

### v4.0.7

- 将 `Button` 组件的 `style` 属性更改为 `variant` 属性。这可以帮助你添加自定义样式而不产生冲突。你应该这样更改代码：

  ```jsx
  <Button title='My Button' style='primary' /> // [!code --]
  <Button title='My Button' variant='primary' /> // [!code ++]
  ```

- 更改了 `src/site.config.ts` 中的 `ExternalLinks` 配置。你应该这样更改配置：

  ```ts title="src/site.config.ts"
  export const theme: ThemeUserConfig = {
    content: {
      externalLinksContent: ' ↗', // [!code --]
      /** 外部链接配置 */ // [!code ++]
      externalLinks: { // [!code ++]
        content: ' ↗', // [!code ++]
        /** 外部链接元素的属性 */ // [!code ++]
        properties: { // [!code ++]
          style: 'user-select:none' // [!code ++]
        } // [!code ++]
      }, // [!code ++]
      /** 博客分页大小（可选） */
      blogPageSize: 8,
      externalLinkArrow: true // 显示外部链接箭头 // [!code --]
    }
  }
  ```

  - 将 Waline 文件移出包！查看 [Commit 5119694](https://github.com/cworld1/astro-theme-pure/commit/5119694ab3e32b600887dca30650bcbc63da3f7b) 了解如何更改现有页面和布局。别忘了将 `src/components/waline` 中的文件复制到你的项目中。

### v4.0.6

- 将项目卡片变量名从 `project` 更改为 `projects`。

### v4.0.5

- 将 UnoCSS 预设从 `Wind3` 更改为 `Mini`。一些功能被移除，如 `gradient`、`animation`、`container` 等。

### v4.0.3

- 将页脚注册配置更改为 `links`。这将给你更多灵活性来添加更多链接或其他信息：

  ```ts title="src/site.config.ts"
  export const theme: ThemeUserConfig = {
    footer: {
      links: [ // [!code highlight:12]
        {
          title: '萌ICP备 114514号',
          link: 'https://icp.gov.moe/?keyword=114514',
          style: 'text-sm' // Uno/TW CSS 类
        },
        {
          title: '站点条款',
          link: '/terms/list',
          pos: 2 // position 设为 2 将附加到版权行
        }
      ]
    }
  }
  ```

### v4.0.2-beta

- 考虑将工具图标移动到新路径如 `src/assets/tools/`；这可以帮助用户知道哪些与工具相关。
- 将友链 `applyTip` 更改为自定义数组样式：

  ```ts title="src/site.config.ts"
  export const integ: IntegrationUserConfig = {
    links: {
      applyTip: [
        { name: '名称', val: theme.title },
        { name: '描述', val: theme.description || '无' },
        { name: '链接', val: 'https://astro-pure.js.org/' },
        { name: '头像', val: 'https://astro-pure.js.org/favicon/favicon.ico' }
      ]
    }
  }
  ```

- 添加环境变量 `BUN_LINK_PKG`。将其设置为 `true` 可自动引用 `packages/pure` 路径中的包。
- 将 CSS 框架更改为 UnoCSS。这将影响 `unocss.config.ts` 和排版类名设置。

### v4.0.1-beta

- 将 svg 传递导入更改为自定义导入 svg 元素。参见[用户组件 #Svg 加载器](/docs/integrations/components#svg-loader)了解用法。

### v4.0.0-alpha

- 将所有重用的组件、类型和工具移动到新的 npm 包 `astro-pure`。
- 新格式配置文件 `src/site.config.ts`。
- 将搜索引擎更改为 [pagefind](https://pagefind.app/)。

### v3.1.4

- 将高级组件移动到 `src/components/advanced`。参见[高级组件](/docs/integrations/advanced)。

### v3.1.3

- 将用户组件移动到 `src/components/user`。参见[用户组件](/docs/integrations/components)。

### v3.0.9

一个稳定版本。

## 更新方法

### Rebase

如果你使用 fork 创建新项目，可以直接将主题仓库 rebase 到最新版本。

### 手动合并

由于 git 历史不同，你可能需要手动将主题仓库合并到你的项目中。

对于 Windows 用户，一个实用的方法是使用工具 [WinMerge](https://winmerge.org/)。